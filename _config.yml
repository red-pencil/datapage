title: RDV
description: Ego data for Robot
url: "https://red-pencil.github.io"
baseurl: "/datapage"
lang: en

# Build settings
markdown: kramdown
permalink: pretty

# Exclude from processing
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules

# Defaults
defaults:
  - scope:
      path: ""
    values:
      layout: default